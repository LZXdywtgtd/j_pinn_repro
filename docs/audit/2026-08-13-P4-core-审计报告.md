# 2026-08-13 P4-core 自我审计报告

> 项目：J-PINN-Repro
> 阶段：阶段2 审计代码（P4-core: Checkpoint Resume 实现）
> 时间：2026-08-13
> 审计员：Claude（自我审计）
> 依据：`docs/全流程.md` 阶段2 4 维度审计框架

---

## 一、审计对象

- `train.py`（新增 `--resume` flag + 续训逻辑，line 96-255）
- `tests/smoke_test.py`（新增 Step 8 含续训 state + Step 9 RNG roundtrip，line 121-172）

---

## 二、4 维度审计报告

### 1. 准确性

| Bug ID | 优先级 | 行号 | 问题 | 根因 | 影响范围 |
|---|---|---|---|---|---|
| **A1** | CRITICAL | train.py:225, 333-336 | 续训后只跑 1 epoch 就退 | 保存 `"epoch": args.epochs`（目标），续训 `start_epoch = target+1`，for 循环 range(target+1, target+1) 仅 1 步 | 所有用户用 `--resume` 都立即退出 |
| **A2** | CRITICAL | train.py:139-141, 219 | scheduler.T_max 不对齐 | scheduler 创建用新 `args.epochs`，但 resume 时仅 load_state_dict，未用 checkpoint 内的 target_epochs 覆盖 | 变更 epochs 的续训场景——LR 调度错乱 |
| **A3** | CRITICAL | train.py:139-141, 219 | scheduler `last_epoch` 默认 0 与 load 后的 last_epoch 冲突 | CosineAnnealingLR 内部 `last_epoch` 是已完成 step 数；load_state_dict 后到第一次 step() 之间状态不一致 | 续训后前几个 epoch LR 可能突变 |
| **A4** | MEDIUM | tests/smoke_test.py:147-172 | Step 9 只 mock RNG roundtrip，未真正调 `train.py --resume` | smoke test 偷懒，省略 subprocess 验证 | 回归保护弱——未来 train.py 改动可能破坏 resume 但 smoke 仍通过 |
| **A5** | LOW | tests/smoke_test.py:123-140 | Step 8 未验证 rng_state 值有效性（仅验证 key 存在） | 测试覆盖范围不足 | 影响极小（Step 9 已覆盖） |
| **A6** | LOW | docs/api.md / CHANGELOG.md | `--resume` flag + checkpoint schema 变化未说明 | 文档同步滞后 | 用户使用 `--resume` 时找不到文档 |

### 2. 完整性

| Bug ID | 优先级 | 行号 | 问题 |
|---|---|---|---|
| **C1** | MEDIUM | （同 A4） | Step 10 端到端 resume 测试缺失 |

### 3. 一致性

| Bug ID | 优先级 | 行号 | 问题 |
|---|---|---|---|
| **D1** | LOW | train.py:189-196 | 干跑污染 Adam optimizer 状态（v4 用 dry_run_state 回滚） |

### 4. 时效性

| Bug ID | 优先级 | 行号 | 问题 |
|---|---|---|---|
| **T1** | MEDIUM | docs/api.md §5.1 | `--resume` 未列出 |
| **T2** | MEDIUM | CHANGELOG.md | v0.4 段未添加 |

---

## 三、修复优先级

### CRITICAL（必须修）
1. A1：epoch 语义错
2. A2：scheduler.T_max 不对齐
3. A3：scheduler last_epoch 错位

### HIGH（必须修）
- 无

### MEDIUM（应该修）
4. C1 / A4：Step 10 端到端续训测试
5. T1 / T2：文档同步

### LOW（建议修，记入 CODE_BUGS）
6. D1：干跑污染 optimizer
7. A5：Step 8 rng_state 覆盖加强
8. A6：文档滞后

---

## 四、修复计划（按 阶段3 自动修复）

### Fix A1：epoch 语义错
- **方案**：保存 `"epoch": completed_epochs`（最后一次跑的 epoch 数）；续训 `start_epoch = completed_epochs + 1`
- **触发点**：`train.py:331-352` 保存处、`train.py:225` 续训读取处
- **向后兼容**：旧 checkpoint（`"epoch": args.epochs`）→ 检测 + 警告

### Fix A2 + A3：scheduler 对齐
- **方案**：用 checkpoint 内 `args.epochs` 覆盖当前 `args.epochs`；重建 scheduler；load_state_dict
- **触发点**：`train.py:139-141, 219`
- **向后兼容**：旧 checkpoint 无 `args["epochs"]` 字段 → 跳过覆盖

### Fix C1 / A4：Step 10 端到端续训
- **方案**：用 subprocess 调 `python train.py --resume <ckpt> --epochs 200` + 验证 CSV 行数
- **触发点**：`tests/smoke_test.py` 新增 Step 10
- **影响**：冒烟测试时长 + 几秒

### Fix T1 / T2：文档同步
- **方案**：
  - `docs/api.md` §5.1 加 `--resume` flag
  - `CHANGELOG.md` 加 v0.4 段（描述 P4-core 实现）
- **触发点**：两文件修改
- **影响**：伴随性小文档更新，合并入 code commit

---

## 五、保留到 v0.5

- D1（干跑污染）：记入 `CODE_BUGS.md` 作为已知未修
- A5（Step 8 覆盖加强）：记入 `CODE_BUGS.md`
- A6（文档滞后通用问题）：建立"每次 commit 前审计文档"机制（已在全流程.md 体现）

---

## 六、审计结论

**P4-core 当前实现有 3 个 CRITICAL bug 必须修复**才能 commit。所有问题均已识别并附修复方案。下一步：阶段3 自动修复。