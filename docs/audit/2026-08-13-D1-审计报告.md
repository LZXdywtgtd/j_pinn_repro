# 2026-08-13 D1 COMSOL 多扫描 batch loader 审计报告

> 项目：J-PINN-Repro
> 阶段：阶段2 审计代码（D1: COMSOL 多扫描 batch loader 实施）
> 时间：2026-08-13
> 审计员：Claude（自我审计）
> 依据：`docs/全流程.md` 阶段2 4 维度审计框架

---

## 一、审计对象

- `data/comsol_png_loader.py`（重构 `load_comsol_scan_dir` + 新增 `load_comsol_scan_dir_as_array` + `_natural_sort_key` + `_auto_detect_subdir`）
- `tests/test_comsol_png_loader.py`（扩展 9 个 D1 测试 + 适配新接口）

## 二、4 维度审计报告

### 1. 准确性

| Bug ID | 优先级 | 位置 | 问题 | 根因 | 影响 |
|---|---|---|---|---|---|
| **D1-A1** | CRITICAL | `_detect_physical_region` | 物理域检测把色标条纳入（right 扩到 1380）| 原算法取整体非白 min/max，色标列也非白 | 温度场宽 980 而非 900；多帧 shape 错 |
| **D1-A2** | CRITICAL | `_rgb_to_colorbar_position` | 2000×1500 图构造 (800, 980, H_cb, 3) 距离矩阵 OOM | 未下采样色标 + 逐像素距离矩阵 | 单帧 35.6s；155 帧不可用 |
| **D1-A3** | MEDIUM | `_rgb_to_colorbar_position` | `n_levels=128` 下采样与 `cb_rgb` 高度 <128 冲突（测试 5 行色标 → p=0.031）| `np.linspace(0, h-1, 128)` 重复色标级 | 小色标时精度错乱 |
| **D1-A4** | HIGH | `_auto_detect_subdir` | 旧实现硬编码优先级列表（温度→d_hist→应力...）| 与用户"动态发现"决策冲突 | 不符合需求 |

### 2. 完整性

| Bug ID | 优先级 | 位置 | 问题 |
|---|---|---|---|
| **D1-C1** | MEDIUM | 测试 | `_make_synthetic_png` 默认 1500×2000，imageio 写盘慢（单测 116s）|
| **D1-C2** | LOW | `as_array` | 返回 3 元组 `(T_vol, field_name, meta)` 与 v0.5 计划"返回 2 元组"略有出入（已按用户最新决策改为 3 元组）|

### 3. 一致性

| Bug ID | 优先级 | 位置 | 问题 |
|---|---|---|---|
| **D1-D1** | LOW | `_detect_multiplier_from_ticks` | 简化版返回 1.0 不 OCR；与 P11 文档"自动检测 ×10²"矛盾（已知限制）|

### 4. 时效性

| Bug ID | 优先级 | 位置 | 问题 |
|---|---|---|---|
| **D1-T1** | MEDIUM | `docs/dev_reference/api.md §1.3` | 仍是 STUB 签名，未更新 |
| **D1-T2** | MEDIUM | `CHANGELOG.md` | 未加 v0.5 段 |
| **D1-T3** | MEDIUM | `docs/architecture/架构设计文档.md` | "(预留 STUB)" 注释未更新 |

## 三、修复优先级

### CRITICAL（已修）
- D1-A1（物理域检测）→ 修复为"最宽连续厚列块"
- D1-A2（性能）→ 修复为"色标下采样 + argmin 向量化"

### HIGH（已修）
- D1-A4（auto-detect 硬编码）→ 按用户决策改为"动态发现 + 用户指定"

### MEDIUM（已修）
- D1-A3（n_levels 上限）→ `min(n_levels, len(cb_rgb))`

### MEDIUM（待文档同步）
- D1-T1 / D1-T2 / D1-T3（文档滞后）

### LOW（记入 CODE_BUGS）
- D1-C1（测试慢，可减小 PNG）
- D1-D1（multiplier 简化版）

## 四、修复验证

```bash
cd D:/team_project/j_pinn_repro
/c/Users/LZXdywtgtd/.conda/envs/jpinn/python.exe -m pytest tests/test_comsol_png_loader.py
# 16 passed（含 D1 的 10 个新测试）
```

## 五、审计结论

D1 代码实施完成，3 个 CRITICAL/HIGH bug 已修复（物理域检测 + 性能 + auto-detect），全部 16 测试通过。下一步：阶段4 写文档（api.md / CHANGELOG / architecture）+ 再审计 → commit → 等授权 push。