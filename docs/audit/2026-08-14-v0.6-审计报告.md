# 2026-08-14 v0.6 开发类任务审计报告

> 项目：J-PINN-Repro
> 阶段：阶段2 审计代码（v0.6 7 个开发项：D1-fix/P7/P17/P12/P6/P9/D3）
> 时间：2026-08-14
> 审计员：Claude（自我审计，全量重读文件）
> 依据：`docs/全流程.md` 阶段2 4 维度审计框架

---

## 一、审计对象

7 个 commit（`870c468` ~ `7b53ab4`）涉及文件：
- `train.py`（D1-fix/P7/P17/P12/P6/D3 全部触碰）
- `losses.py`（P12）
- `models/pinn_core.py`（P6）
- `schedulers.py`（P17，新）
- `run_ensemble.py`（P9，新）
- `logging_utils.py`（D3，新）
- `collect_pareto.py`（P6，新）
- `utils_tee_eta.py`（P7）
- `tasks/architecture_sweep_recipes.json`（P6，新）
- `tests/test_scheduler.py`、`tests/test_ensemble.py`（新）

## 二、4 维度审计报告

### 1. 准确性

| Bug ID | 优先级 | 位置 | 问题 | 修复 |
|---|---|---|---|---|
| **A1** | CRITICAL | `logging_utils.py:log_metrics` | tensorboard 双前缀（metrics key 已带 `loss/`，`add_scalars("loss",...)` 又加一次 → `loss/loss/total`）| ✅ 去掉 key 前缀，wandb 单独加前缀 |
| **A2** | HIGH | `schedulers.py:get_lr/step` | `step()` 直接改 `group["lr"]`，`get_lr()` 只读——违反 `_LRScheduler` 基类契约 | ✅ 改为 `step()` 只更新状态，`get_lr()` 计算，遵循契约 |
| **A3** | MEDIUM | `run_ensemble.py:average_models` | `.float()` 把 float64 降为 float32 | ✅ 去掉 `.float()`，保持原 dtype |
| **A4** | MEDIUM | `collect_pareto.py` | `params` 字段恒为 0（n_params_map 从不填充）| ✅ 从 checkpoint 读 `n_params` |

### 2. 完整性

| Bug ID | 优先级 | 问题 | 修复 |
|---|---|---|---|
| **A5** | MEDIUM | 文档严重滞后：api.md CLI 表缺 9 flag，CHANGELOG 无 v0.6 段，架构文档未登记 6 新文件，调参指南无 P17/P12 | ✅ 本次补写（见下） |

### 3. 一致性

- ✅ 7 个 commit 均独立 + push，符合"一个 commit 推一次"
- ✅ 复用 v4 模式（run_ablations subprocess、estimate_training_time、dry-run 快照）
- ⚠️ P17 用 `_LRScheduler` 但最初违反基类契约（已修 A2）

### 4. 时效性

- ❌ 文档滞后（A5），本次全量补写

## 三、修复验证

```bash
python -m pytest tests/test_scheduler.py tests/test_ensemble.py -v
# 7 passed
python tests/smoke_test.py
# ALL SMOKE TESTS PASSED（11 步）
```

## 四、审计结论

7 个任务代码实现正确，但发现 4 个代码 bug（1 CRITICAL + 1 HIGH + 2 MEDIUM）+ 1 个文档遗漏。全部修复后测试通过。文档同步见后续 `docs:` commit。